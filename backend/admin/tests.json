{
  "tests": [
    {
      "name": "Access denied without token",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ только для администратора"},
      "bodyMatcher": "partial"
    }
  ]
}